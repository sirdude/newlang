#!/usr/bin/sweet
# The Computer Language Benchmarks Game
# http://shootout.alioth.debian.org/

void do_work(int x, int y) {
	int h, w;

	h=0;
	while (h < y) {
		w = 0;
		while (w < x) {

# XXX 
			w++;
		}
		h++;
	}
}

int main() {
	w = ARGV[0];
	if (!w) {
		w= 60;
	}
	h = w;

	write "P4\n" + w + " " + h + "\n";

	do_work(h,w);

	return 1;
}

