#!/usr/bin/sweet
# http://www.bagley.org/~doug/shootout/

int fib(int n) {
	if (n < 2) {
		return 1;
	}

	return( fib(n-2) + fib(n-1) );
}

int main() {
	int n;

	n = argv[1];

	if (n < 1) {
		n = 1;
	}

	write(fib(n) + \n");
}
